<main>
<section>
	<h2>Create a new Unit</h2>

	<%= form_for @unit, url: {action: "create"} do |f| %>
		Name: <%= f.text_field :name %>
		Combat-Type: <%= f.select :combatType, ["Infantry", "Cavalry", "Artillery", "Other"] %>
		Description: <%= f.text_area :description %>
		Price: <%= f.number_field :price, within: 1..1000 %>
		Tier: <%= f.select :tier, [1, 2, 3] %>
		Hero: <%= f.check_box :hero, checked: false %>
		<%= f.submit "Add Unit" %>
	<% end %>

	<ul class="errors">
		<% @errors.each do |error| %>
			<li><%= error %></li> 
		<% end if @errors != nil %>
	</ul>

</section>
</main>